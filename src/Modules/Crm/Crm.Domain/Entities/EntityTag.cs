using Nexcore.SharedKernel;

namespace Crm.Domain.Entities
{
    /// <summary>
    /// Polymorphic junction linking a Tag to any CRM entity.
    /// Allows tagging of Leads, Contacts, Accounts, Deals, Cases, etc.
    /// </summary>
    public class EntityTag : BaseEntity
    {
        // ??? Keys ??????????????????????????????????????????????????????
        public Guid TagId { get; set; }
        public Tag Tag { get; set; } = null!;

        // ??? Polymorphic Target ????????????????????????????????????????
        public Guid EntityId { get; set; }
        public string EntityType { get; set; } = string.Empty;  // "Lead", "Contact", "Account", "Deal", "Case", "Campaign"
    }
}
