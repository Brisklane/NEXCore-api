using Nexcore.SharedKernel;

namespace Crm.Domain.Entities
{
    /// <summary>
    /// A comment or reply on a support case.
    /// Aligned with Salesforce CaseComment, Dynamics Case Resolution, Zendesk Ticket Comment.
    /// </summary>
    public class CaseComment : BaseEntity
    {
        // ??? Keys ??????????????????????????????????????????????????????
        public Guid CaseId { get; set; }
        public Case Case { get; set; } = null!;

        // ??? Content ???????????????????????????????????????????????????
        public string CommentBody { get; set; } = string.Empty;

        // ??? Visibility ????????????????????????????????????????????????
        public bool IsPublished { get; set; }   // Visible to customer (portal)
        public bool IsInternal { get; set; }    // Internal agent note

        // ??? Author ????????????????????????????????????????????????????
        public Guid? AuthorId { get; set; }     // FK to User
        public bool IsCustomerComment { get; set; }  // Comment came from customer portal
    }
}
