using Nexcore.SharedKernel;

namespace Crm.Domain.Entities
{
    /// <summary>
    /// A label/tag that can be applied to any CRM record for categorization.
    /// Aligned with HubSpot Tags, Dynamics Labels, Salesforce Topics.
    /// </summary>
    public class Tag : BaseEntity
    {
        // ??? Basic Info ????????????????????????????????????????????????
        public string TagName { get; set; } = string.Empty;  // Required, unique
        public string? Color { get; set; }                   // Hex color code for UI

        // ??? Navigation ????????????????????????????????????????????????
        public ICollection<EntityTag> EntityTags { get; set; } = new List<EntityTag>();
    }
}
